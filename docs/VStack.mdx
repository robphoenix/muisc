---
name: VStack
route: /vstack
---

import Box from '@material-ui/core/Box'
import { VStack } from '../src'
import { Wrapper, Item } from './components.tsx'

# VStack

<Wrapper>
  <VStack spacing={3}>
    {Array.from({ length: 4 }).map((item, i) => (
      <Item key={i} />
    ))}
  </VStack>
</Wrapper>

## Spacing

<Wrapper>
  <VStack spacing={2} align="center">
    {Array.from({ length: 4 }).map((item, i) => (
      <Item key={i} sx={{ width: (i + 1) * 100 }} />
    ))}
  </VStack>
</Wrapper>

## Horizontal Alignment

<Wrapper>
  <VStack spacing={4} dividers>
    <VStack spacing={2} align="left">
      <Item sx={{ width: '6rem' }} />
      <Item sx={{ width: '8rem' }} />
      <Item sx={{ width: '6rem' }} />
    </VStack>
    <VStack spacing={2} align="center">
      <Item sx={{ width: '6rem' }} />
      <Item sx={{ width: '8rem' }} />
      <Item sx={{ width: '6rem' }} />
    </VStack>
    <VStack spacing={2} align="right">
      <Item sx={{ width: '6rem' }} />
      <Item sx={{ width: '8rem' }} />
      <Item sx={{ width: '6rem' }} />
    </VStack>
  </VStack>
</Wrapper>

## Dividers

<VStack spacing={5}>
  <Wrapper>
    <VStack spacing={4} dividers dividerProps={{ light: true }}>
      <Item />
      <Item />
      <Item />
    </VStack>
  </Wrapper>
  <Wrapper>
    <VStack spacing={4} dividers dividerProps={{ variant: 'middle' }}>
      <Item />
      <Item />
      <Item />
    </VStack>
  </Wrapper>
</VStack>
